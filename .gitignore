# Ignore everything by default
*

# Allow C# source files and meta files
!*.cs
!*.cs.meta

# Allow Java source files and meta files
!*.java
!*.java.meta

# Ensure directories are tracked (but only with allowed files inside)
!.gitignore
!.gitattributes
